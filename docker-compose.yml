version: '2'
services:
  tg_dj:
    build: .
    volumes:
      - ./data:/data
    stop_grace_period: 30s
  nginx:
    image: nginx
    ports:
      - 8080:80
    volumes:
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
